---
name: build-makemkvcon
run-name: Build MakeMKV CLI Software Container
on: [ workflow_dispatch ]

# variables
env:
  MAKEMKV_VERSION: 1.18.4
  FFMPEG_BRANCH: release/8.1

  IMAGE_NAME: makemkvcon
  IMAGE_TAGS: v1 ${{ github.sha }}
  IMAGE_REGISTRY: ghcr.io/${{ github.repository_owner }}
  REGISTRY_USER: ${{ github.actor }}
  REGISTRY_PASSWORD: ${{ github.token }}

permissions:
  contents: read
  packages: write

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Clone workspace for makemkv-container
        uses: actions/checkout@v7

      - name: Update package metadata
        run: |
          sudo apt -y update

      - name: Install container build/run tools
        run: |
          sudo apt -y install \
            ansible \
            podman \
            buildah

      - name: Test Ansible
        run: ansible localhost -m setup

      - name: Install Build Tools
        run: |
          ansible-playbook build-makemkvcon-pb.yaml

      # And build makemkvcon - separate?
      #
      # Now create a container from build/model

      - name: Create Container Image from Model
        run: |
          scripts/minimal-makemkv.sh

      - name: Confirm container image creation
        run:
          buildah images

      - name: Push new image to registry
        uses: redhat-actions/push-to-registry@v3
        with:
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ env.IMAGE_TAGS }} 
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_PASSWORD }}

